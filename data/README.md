# data/

This tool takes equations as command-line arguments, so it needs no data files
to run.

If you want to batch-check many equations, put a plain-text file here with one
equation per line and loop over it in your shell. Anything you add here (except
this README) is gitignored.
